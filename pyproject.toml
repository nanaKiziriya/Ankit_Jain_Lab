# file: pyproject.toml

[build-system]
requires = [
  "setuptools>=65",
  "wheel>=0.38"
]
build-backend = "setuptools.build_meta"

[project]
name = "ajlab"
version = "0.1.0"
dependencies = [
  "matplotlib",
  "numpy",
  "pandas"
]
